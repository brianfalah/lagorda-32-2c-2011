

#ifndef LISTAPPD_H_
#define LISTAPPD_H_
#include <stdint.h>




/*
lPeticiones *lAgregar(lPeticiones **p, int desc,uint32_t sectorLogico,uint32_t pista, uint32_t sectorEnPista, char* buf, int buflen, uint8_t tPet);
void lSacar(lPeticiones **p);
void lSacarAll(lPeticiones **p);
lPeticiones **lBuscarPorFd(lPeticiones **n, int desc);
lPeticiones **lBuscarPorSector(lPeticiones **n, uint32_t sect);
void lImprimir(lPeticiones *n);
void testingCargarPedidos(lPeticiones **p);
int pruebaLista(lPeticiones **p);
*/

#endif /* LISTAPPD_H_ */
